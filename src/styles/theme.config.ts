import { Theme } from '@radix-ui/themes';
// import '@radix-ui/themes/styles.css';

export const themeConfig = {
  accentColor: '#101437',
  // accentColor: 'brown',
  grayColor: 'sage',
  scaling: '100%',
  radius: 'small',
} as const;

export { Theme };
