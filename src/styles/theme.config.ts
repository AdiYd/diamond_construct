import { Theme } from '@radix-ui/themes';
// import '@radix-ui/themes/styles.css';

export const themeConfig = {
  // accentColor: '#ddd6d6',
  grayColor: 'sage',
  scaling: '100%',
  radius: 'medium',
} as const;

export { Theme };
